import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 52,
    height: 52,
    marginRight: 24,
    borderRadius: 8
  },
  avatar: {
    width: 46,
    height: 46,
    borderRadius: 8
  }
});
